matriz = []

for lin in range(20): 
    linha = []
    for col in range(20): 
        linha.append(int(input("")))
    matriz.append(linha)
    
for lin in range(20): 
    for col in range(20): 
        
